import 'package:flutter/material.dart';

class AddGoalsPage extends StatelessWidget {
  const AddGoalsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}